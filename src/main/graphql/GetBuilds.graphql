query GetBuilds($projectId: String!, $version: String!) {
    project(key: $projectId) {
        version(key: $version) {
            builds(last: 100) {
                nodes {
                    number
                }
            }
        }
    }
}